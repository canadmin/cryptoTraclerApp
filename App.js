import * as React from 'react';
import MainContainer from "./src/navigation/MainContainer";
import {
  SafeAreaView,
} from 'react-native';

const App = () => {
  return(
    <MainContainer/>
  )
};

export default App;
